# Tailscale DERP Server Configuration
# 复制此文件为 .env 并根据你的需求修改配置

# DERP 服务器主机名或 IP 地址（必须修改为你的服务器 IP）
DERP_HOSTNAME=88.88.88.88

# DERP HTTP 服务端口
DERP_HTTP_PORT=9003

# STUN 服务端口
DERP_STUN_PORT=9004

# 是否验证客户端（通常设置为 false）
# true: 启用客户端验证
# false: 禁用客户端验证
DERP_VERIFY_CLIENTS=false

# 证书模式
# manual: 使用自签名证书（推荐用于 IP 部署）
# letsencrypt: 使用 Let's Encrypt 证书（需要域名）
DERP_CERT_MODE=manual

# 证书存储目录（容器内路径，通常不需要修改）
DERP_CERT_DIR=/var/lib/derper

# 日志级别
# info: 标准日志输出
# debug: 详细调试信息
DERP_LOG_LEVEL=info